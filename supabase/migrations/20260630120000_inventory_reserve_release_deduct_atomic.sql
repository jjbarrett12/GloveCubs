-- =============================================================================
-- Superseded (pooler-safe split): inventory checks + legacy bigint RPCs moved to
-- 20260630120001–20260630120014. Original single-file migration is not re-run;
-- logic is unchanged on those paths. Git history retains prior contents.
-- =============================================================================
SELECT 1;
