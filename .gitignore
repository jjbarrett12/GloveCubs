# Dependencies & build output
**/node_modules/
**/.next/
**/*.tsbuildinfo

# Local tooling (git worktrees / agent paths — never commit)
.claude/worktrees/

*.db
.env
.env.*
!.env.example
!**/.env.example
.DS_Store
Thumbs.db
# Supabase CLI local artifacts
supabase/.temp/
# Scheduled Fishbowl customer export (generated every 30 min)
data/

# Legacy JSON database - fully migrated to Supabase; do not recreate
database.json
database_backup.json
contamination-report.json
contamination-flagged.csv
quarantine-plan.json
quarantine-plan.csv
.vercel
.env*.local

# Local storefront scratch / smoke artifacts
storefront/tmp-*
test-results/
playwright-report/
coverage/
